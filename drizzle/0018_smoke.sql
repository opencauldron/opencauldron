-- ============================================================================
--  Migration 0018 — DOCKER UPGRADE-PATH SMOKE TEST.
--  DO NOT use this file as a template for real migrations.
-- ============================================================================
--
-- This migration exists ONLY to validate that `docker compose pull && docker
-- compose up -d` applies new migrations on an existing volume without losing
-- data. It is the canonical regression for spec SC-002 (specs/self-host-docker,
-- task T019). The body is `SELECT 1`, a portable no-op chosen so the file:
--
--   * forces drizzle's migrator to record a row in
--     `drizzle.__drizzle_migrations` (proving the upgrade path runs new
--     migrations end-to-end), AND
--   * makes zero schema changes, so it's harmless on fresh installs and on
--     databases that have already applied it.
--
-- IMPORTANT FOR FUTURE CONTRIBUTORS — DO NOT COPY THIS PATTERN:
--   * Real migrations are generated by `pnpm exec drizzle-kit generate` from
--     `src/lib/db/schema.ts` changes. They contain real DDL.
--   * Real migrations should NEVER be authored by hand-writing a SQL file
--     and adding it to the journal manually.
--   * Real migrations should NEVER use `SELECT 1` as a placeholder. If you
--     don't have schema changes, you don't need a migration.
--   * Index your next migration as 0019.
SELECT 1;
