/**
 * /brands/[slug]/campaigns — Phase 9 will fill this surface in. For now,
 * point users at a coming-soon panel rather than 404-ing.
 */

export default function BrandCampaignsPage() {
  return (
    <div className="rounded-lg border border-dashed px-6 py-12 text-center text-sm text-muted-foreground">
      Campaigns ship in the next phase.
    </div>
  );
}
